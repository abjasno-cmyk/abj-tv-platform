import { MujVeroxTemplate } from "@/components/abj/MujVeroxTemplate";

export const dynamic = "force-dynamic";

// MŮJ VEROX dle klientské šablony — komunitní diskuze (formulář + vzkazy).
export default function MujVeroxPage() {
  return <MujVeroxTemplate />;
}
